# Maven 每次构建都能重新生成 target；提交二进制会放大仓库并制造无意义 diff。
target/
# IDEA 的本机窗口、索引等状态不属于团队源码，同一项目在每台机器上都不同。
.idea/
# IDEA 模块文件含本机路径和个人设置，应由 pom.xml 重新导入生成。
*.iml
# .env 通常保存真实密钥或私人地址；配置只提交变量名，值留在本地环境。
.env
# 日志是运行结果而非源码，还可能包含请求或异常信息，不能进入 Git 历史。
*.log
