<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.PaymentWebhookEventMapper">

    <!--
      先落地 webhook 事件，并通过唯一键去重：
      (provider, provider_event_id) 相同就视为重复投递。
    -->
    <insert id="insertIgnore" parameterType="PaymentWebhookEvent" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO payment_webhook_event (
            provider,
            provider_event_id,
            event_type,
            payload_json,
            processed,
            created_at,
            processed_at
        ) VALUES (
            #{provider},
            #{providerEventId},
            #{eventType},
            #{payloadJson},
            #{processed},
            #{createdAt},
            #{processedAt}
        )
    </insert>

    <!--
      业务推进成功后再打 processed=1。
      这样可以保证“先去重，再处理，再标记完成”的顺序清晰可追踪。
    -->
    <update id="markProcessed">
        UPDATE payment_webhook_event
        SET processed = 1,
            processed_at = #{processedAt}
        WHERE id = #{id}
          AND processed = 0
    </update>

</mapper>
